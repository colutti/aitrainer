import { describe, it } from 'vitest';

describe('Workouts UI', () => {
  it.skip('unit tests for specific Workouts UI selectors are legacy. Use E2E for visual validation.', () => {
    // Tests skipped because of radical UI transformation to Premium Deep Space
  });
});
